export const PRINTER_WIDTH = 384;

/** Default body text size for composed print jobs (58mm / 384px). */
export const DEFAULT_COMPOSE_FONT_SIZE = 22;

export const BLE_CHUNK_SIZE = 182;
export const BLE_CHUNK_DELAY_MS = 40;
export const BLE_FLUSH_DELAY_MS = 1500;
